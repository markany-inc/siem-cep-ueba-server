# SafePC SIEM 환경 설정

# -- 기존 설정 --
OPENSEARCH_URL=http://203.229.154.49:49200
DASHBOARD_URL=http://203.229.154.49:48501
FLINK_SQL_GATEWAY=http://203.229.154.49:48083
TIMEZONE=Asia/Seoul

# -- 코드 호환성을 위해 이름이 변경되거나 추가된 설정 --

# Flink Rest API 주소 (기존 FLINK_URL)
FLINK_REST_API=http://203.229.154.49:48081

# Kafka Bootstrap 서버 주소 (기존 KAFKA_BOOTSTRAP)
KAFKA_BOOTSTRAP_SERVERS=43.202.241.121:44105

# Kafka Consumer Group Prefix (기본값: siem)
KAFKA_CONSUMER_GROUP_PREFIX=siem

# -- CEP/UEBA 이벤트 구독을 위한 공통 토픽 목록 --
# 쉼표(,)로 구분하여 구독할 토픽을 지정합니다.
KAFKA_EVENT_TOPICS=MESSAGE_AGENT,MESSAGE_DEVICE,MESSAGE_NETWORK,MESSAGE_PROCESS,MESSAGE_PRINT,MESSAGE_DRM,MESSAGE_CLIPBOARD,MESSAGE_CAPTURE,MESSAGE_PC,MESSAGE_SCREENBLOCKER,MESSAGE_ASSETS

# CEP가 생성한 Alert를 저장할 Kafka 토픽 (기본값: cep-alerts)
KAFKA_ALERT_TOPIC=cep-alerts

# 인덱스 접두어 (멀티테넌시 지원)
INDEX_PREFIX=safepc
